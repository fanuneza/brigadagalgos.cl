---
import "../styles/components/page-hero.css";
import TrackedLink from "./TrackedLink.astro";

interface Props {
  eyebrow: string;
  h1: string;
  lead?: string;
  leadBold?: string;
  /** Emphasis of the whole hero. `lead` is the historical rendering. */
  weight?: "lead" | "support" | "quiet";
  ctaText?: string;
  ctaHref?: string;
  ctaVariant?: "primary" | "secondary" | "ghost";
  ctaTrackLabel?: string;
  ctaTrackLocation?: string;
  ctaTrackCategory?: string;
}

const {
  eyebrow,
  h1,
  lead,
  leadBold,
  weight = "lead",
  ctaText,
  ctaHref,
  ctaVariant = "ghost",
  ctaTrackLabel,
  ctaTrackLocation,
  ctaTrackCategory,
} = Astro.props;
---

<section class:list={["page-hero", "section-padding", `page-hero--${weight}`]}>
  <div class="container container--narrow">
    <p class:list={["eyebrow", `eyebrow--${weight}`]}>{eyebrow}</p>
    <h1>{h1}</h1>
    {lead && <p class="page-hero__lead t-lead">{lead}</p>}
    {leadBold && <p class="page-hero__lead page-hero__lead--bold t-lead t-lead-strong">{leadBold}</p>}
    {
      ctaText && ctaHref && (
        <TrackedLink
          href={ctaHref}
          class={`btn btn--${ctaVariant}`}
          trackEvent="cta_click"
          trackLabel={ctaTrackLabel ?? ctaText}
          trackLocation={ctaTrackLocation ?? ""}
          trackDestination={ctaHref}
          trackCategory={ctaTrackCategory}
        >
          {ctaText}
        </TrackedLink>
      )
    }
  </div>
</section>
